<!-- start: header -->
<header class="header">
	<div class="logo-container">
		<a href="{{ path('experimentation_index') }}" class="logo">
			<img src="{{ asset('images/Logo_INRAE.png')}}" height="35" alt="INRA">
		</a>
		<div class="visible-xs toggle-sidebar-left" data-toggle-class="sidebar-left-opened" data-target="html" data-fire-event="sidebar-left-opened">
			<i class="fa fa-bars" aria-label="Toggle sidebar"></i>
		</div>
	</div>

	<!-- start: search & user box -->
	<div class="header-right">
		<ul class="notifications">
			<h5>
				<strong>Mon appli INRAE</h5>
			</ul>

			<span class="separator"></span>
			{% if is_granted('IS_AUTHENTICATED_FULLY') %}

				<div id="userbox" class="userbox">
					<a href="#" data-toggle="dropdown">
						<figure class="profile-picture">
							<img src="{{asset('assets/images/!logged-user.jpg')}}" alt="Joseph Doe" class="img-circle" data-lock-picture="{{asset('assets/images/!logged-user.jpg')}}"/>
						</figure>
						<div class="profile-info" data-lock-name="John Doe" {# data-lock-email="{{app.user.email}}" #}>
							<span class="name">{{app.user.username}}</span>
						</div>

						<i class="fa custom-caret"></i>
					</a>

					<div class="dropdown-menu">
						<ul class="list-unstyled">
							<li class="divider"></li>
							{# <li>
							<a role="menuitem" tabindex="-1" href="pages-user-profile.html"><i class="fa fa-user"></i> Mon compte</a>
							</li> #}
							{% if is_granted('ROLE_SUPER_ADMIN') %}
							<a role="menuitem" tabindex="-1" href="#" target="_blank">
								<i class="fa fa-unlock"></i>
								Administration</a>
							{% endif %}
							<li>
								<a role="menuitem" tabindex="-1" href="{{path('app_logout')}}">
									<i class="fa fa-sign-out"></i>
									Se déconnecter</a>
							</li>
						</ul>
					</div>
				</div>
			{% endif %}
		</div>
		<!-- end: search & user box -->
	</header>
	<!-- end: header -->
