{% extends 'base.html.twig' %}

{% block title %}Sources
{% endblock %}

{% block body %}
    
	<div class="container-fluid">
		<nav aria-label="breadcrumb">
			<ol class="breadcrumb">
				<li class="breadcrumb-item">
					<a href="{{ path('experimentation_index') }}">Experimentations</a>
				</li>
				<li class="breadcrumb-item">
					<a href="{{ path('lot_index', {'idExpe': idExpe}) }}">Lots</a>
				</li>
                <li class="breadcrumb-item">
					<a href="{{ path('bassin_index', {'idExpe': idExpe, 'idLot': idLot}) }}">Bassin</a>
				</li>
				<li class="breadcrumb-item active" aria-current="page">Source</li>
			</ol>
		</nav>

		<table class="table table-bordered table-hover" id="tableLot">
			<thead>
				<tr>
                    <th>Nom</th>
                    <th>Type</th>
                    <th>Debut</th>
                    <th>Fin</th>
                    <th>Debit</th>
                    <th>Valeur</th>
                    <th>Info</th>
                    <th>Date</th>
                    <th>Type</th>
				</tr>
			</thead>
			<tbody>
				{% for source in sources %}

					<tr>
                        <td>{{ source.nomAlimEau }}</td>
                        <td>{{ source.libTypeAlim }}</td>
                        <td>{{ source.debutAlimente }}</td>
                        <td>{{ source.finAlimente }}</td>
                        <td>{{ source.debitAlimEau }}</td>
                        <td>{{ source.reveleAlimentation }}</td>
                        <td>{{ source.infoReleveAlimentation }}</td>
                        <td>{{ source.dateReleveAlimentation }}</td>
                        <td>{{ source.libTypeReleveAlimentation }}</td>
					</tr>

				{% else %}
					<tr>
						<td colspan="25">Aucun résultat</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<br><br>
		<a href="{{ path('bassin_index', {'idExpe': idExpe, 'idLot': idLot}) }}" class="btn btn-danger">Retour</a>
	</div>
{% endblock %}
