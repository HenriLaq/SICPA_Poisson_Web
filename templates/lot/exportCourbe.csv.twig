Lot : {{ nomLot|convert_encoding('ISO-8859-1', 'UTF-8')|raw }};

Date;Temperature;Poids reel; Poids theorique;
{% for c in courbe %}
    {{ c.datePoint ? c.datePoint|date('d-m-Y') : '' }};{{ c.temperature }};{{ c.poidsReel }};{{ c.poidsPrevu }};
{% endfor %}